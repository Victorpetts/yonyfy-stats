import React from "react";

const ButtonRow = props => (
  <div className="buttonRow">

  </div>
);
export default ButtonRow;

// {props.showComp !== 'round' &&
//   <div>
//     <button className={ !props.activeButton ? "activeButton" : "button" } onClick={props.getPlacementsStats}>Placering</button>
//     <button className={ props.activeButton ? "activeButton" : "button" } onClick={props.getSeasonStats}>Poäng</button>
//   </div>
// }
